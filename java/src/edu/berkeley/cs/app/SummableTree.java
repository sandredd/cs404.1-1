package edu.berkeley.cs.app;

import edu.berkeley.cs.util.TreeMap;

public class SummableTree extends TreeMap<Integer, Boolean> {
  /**
   * @param target the sum we are searching for along some path through the tree from root to leaf
   * @return true if there is a path whose sum equals the target
   */
  public boolean hasPathSum(int target) {
    // TODO: complete this function
    return false;
  }
}
